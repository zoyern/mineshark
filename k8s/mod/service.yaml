apiVersion: v1
kind: Service
metadata:
  name: mc-mod
  namespace: mineshark
spec:
  type: ClusterIP
  selector:
    app: mc-mod
  ports:
    - name: minecraft
      port: 25565
      targetPort: 25565
    - name: rcon
      port: 25575
      targetPort: 25575