#Client-only settings
[client]
	#Render Freeze like rage overlay
	renderRageOverlay = true
	#Final opacity of rage overlay
	# Default: 0.5
	# Range: 0.0 ~ 1.0
	rageOverlayOpacity = 0.1
	#Render Rage Bar on experience bar
	renderRageBar = true
	#Render Rage Icon next to player's Hotbar
	renderRageIcon = true
	#Render fire particles around player when rage is active
	renderFireParticles = true
	#Negative offsets to left side, positive to right
	# Default: 0
	# Range: > -2147483648
	offsetX = -1
	#Negative offsets to up, positive to down
	# Default: 0
	# Range: > -2147483648
	offsetY = -16
